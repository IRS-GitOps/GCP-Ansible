kind: Pod
metadata:
  name: ansible
spec:
  containers:
  - name: ansible
    image: johns2te/ansible:ansible2912
    command:
    - cat
    tty: true
    volumeMounts:
    - name: gcloud
      mountPath: "/root/.gcloud/localhost.json"
      readOnly: true
  volumes:
  - name: gcloud
    secret:
      secretName: tjohnson
