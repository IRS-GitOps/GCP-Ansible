kind: Pod
metadata:
  name: ansible
spec:
  containers:
  - name: ansible
    image: johns2te/ansible:sshubuntu
    command:
    - cat
    tty: true
    volumeMounts:
    - name: gcloud
      mountPath: "/root/.gcloud/"
      readOnly: true
    - name: ssh
      mountPath: "/root/.ssh/"
      readOnly: true
  volumes:
  - name: gcloud
    secret:
      secretName: tjohnson
  - name: ssh
    secret:
      name: secret-ssh
      items:
        - key: id_rsa
          mode: 600
